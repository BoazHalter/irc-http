apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "irc.fullname" . }}-test-connection"
  labels:
    {{- include "irc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "irc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
